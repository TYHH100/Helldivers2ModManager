<UserControl x:Class="Helldivers2ModManager.Components.ToastOverlay"
			 x:ClassModifier="internal"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:cmp="clr-namespace:Helldivers2ModManager.Components"
			 mc:Ignorable="d"
			 d:DesignHeight="200"
			 d:DesignWidth="400"
			 IsHitTestVisible="False"
			 UseLayoutRounding="True"
			 TextOptions.TextFormattingMode="Display">
	<!-- 轻量气泡通知：右下角堆叠，自动消失（见 ToastOverlay.xaml.cs），不阻塞任何交互 -->
	<Grid>
		<ItemsControl ItemsSource="{Binding Toasts}"
					  x:Name="ToastsHost"
					  VerticalAlignment="Bottom"
					  HorizontalAlignment="Right"
					  Margin="24">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<StackPanel Orientation="Vertical"/>
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate DataType="{x:Type cmp:ToastItem}">
					<Border Background="#E6141414"
							BorderBrush="{StaticResource MicaBorderBrush}"
							BorderThickness="1"
							CornerRadius="8"
							Padding="14 10"
							Margin="0 6 0 0"
							MaxWidth="360"
							IsHitTestVisible="True"
							Cursor="Hand"
							MouseLeftButtonUp="Toast_Click">
						<StackPanel Orientation="Horizontal">
							<TextBlock FontFamily="Segoe Fluent Icons"
									   FontSize="16"
									   VerticalAlignment="Top"
									   Margin="0 1 10 0">
								<TextBlock.Style>
									<Style TargetType="TextBlock">
										<!-- 成功默认绿色对勾；失败切换红色错误图标 -->
										<Setter Property="Text" Value="&#xE73E;"/>
										<Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
										<Style.Triggers>
											<DataTrigger Binding="{Binding IsError}" Value="True">
												<Setter Property="Text" Value="&#xE711;"/>
												<Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</TextBlock.Style>
							</TextBlock>
							<StackPanel>
								<TextBlock Text="{Binding Title}"
										   FontSize="13"
										   FontWeight="SemiBold"
										   Foreground="{StaticResource NeutralForegroundBrush}"
										   TextTrimming="CharacterEllipsis"/>
								<TextBlock Text="{Binding Message}"
										   FontSize="12"
										   Foreground="{StaticResource NeutralForegroundSecondaryBrush}"
										   TextWrapping="Wrap"
										   Margin="0 2 0 0"
										   Visibility="{Binding Message, Converter={StaticResource StringToVisibilityConverter}}"/>
							</StackPanel>
						</StackPanel>
					</Border>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</Grid>
</UserControl>
