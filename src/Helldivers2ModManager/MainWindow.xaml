<Window x:Class="Helldivers2ModManager.MainWindow"
		x:ClassModifier="internal"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:views="clr-namespace:Helldivers2ModManager.Views"
		xmlns:vms="clr-namespace:Helldivers2ModManager.ViewModels"
		xmlns:cmp="clr-namespace:Helldivers2ModManager.Components"
		xmlns:loc="clr-namespace:Helldivers2ModManager.Extensions"
		mc:Ignorable="d"
		d:DataContext="{d:DesignInstance vms:MainViewModel}"
		Title="{Binding Title}"
		Width="1000"
		Height="700"
		MinWidth="800"
		MinHeight="600"
		Content="{Binding CurrentViewModel}"
		AllowDrop="True"
		PreviewDragOver="Window_PreviewDragOver"
		PreviewDrop="Window_PreviewDrop"
		DragLeave="Window_DragLeave"
		WindowStyle="None"
		WindowStartupLocation="CenterScreen"
		Background="{StaticResource MicaBackgroundBrush}"
		d:Background="{StaticResource MicaBackgroundBrush}">
	<Window.Resources>
		<DataTemplate DataType="{x:Type vms:DashboardPageViewModel}">
			<views:DashboardPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:DeploymentOrderPageViewModel}">
			<views:DeploymentOrderPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:SettingsPageViewModel}">
			<views:SettingsPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:EditPageViewModel}">
			<views:EditPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:ManifestEditPageViewModel}">
			<views:ManifestEditPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:CreatePageViewModel}">
			<views:CreatePageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:TagManagementPageViewModel}">
			<views:TagManagementPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:AutoTagPairingPageViewModel}">
			<views:AutoTagPairingPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:NexusDownloadPageViewModel}">
			<views:NexusDownloadPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:BackgroundTasksPageViewModel}">
			<views:BackgroundTasksPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:ArmorReusePageViewModel}">
			<views:ArmorReusePageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:PatchResourceViewerPageViewModel}">
			<views:PatchResourceViewerPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:ModelPreviewPageViewModel}">
			<views:ModelPreviewPageView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vms:BisectPageViewModel}">
			<views:BisectPageView/>
		</DataTemplate>

	</Window.Resources>

	<Window.Style>
		<Style TargetType="Window">
			<Setter Property="OverridesDefaultStyle" Value="True"/>
			<Setter Property="SnapsToDevicePixels" Value="True"/>
			<Setter Property="BorderBrush" Value="{StaticResource MicaBorderBrush}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Window">
						<Border Name="border"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								Background="{TemplateBinding Background}"
								UseLayoutRounding="True">
							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="48"/>
									<RowDefinition Height="*"/>
								</Grid.RowDefinitions>

								<!-- 自定义背景图片层（半透明露出深色底，保证前景可读） -->
								<Image Grid.RowSpan="2"
									   Source="{Binding BackgroundImageSource}"
									   Stretch="UniformToFill"
									   Opacity="{Binding BackgroundImageOpacity}"
									   IsHitTestVisible="False"
									   RenderOptions.BitmapScalingMode="HighQuality"/>

								<!-- Title Bar with Mica Effect -->
								<Border Grid.Row="0"
										Background="{StaticResource MicaBackgroundBrush}">
									<Border.BorderBrush>
										<LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
											<GradientStop Color="#20FFFFFF" Offset="0"/>
											<GradientStop Color="Transparent" Offset="1"/>
										</LinearGradientBrush>
									</Border.BorderBrush>
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="Auto"/>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="Auto"/>
										</Grid.ColumnDefinitions>

										<!-- Logo and Title -->
										<StackPanel Grid.Column="0"
													Orientation="Horizontal"
													VerticalAlignment="Center"
													Margin="16 0 0 0">
											<Image Source="Resources/Images/logo_icon.png"
												   Width="24"
												   Height="24"
												   Stretch="Uniform"
												   Margin="0 0 12 0"/>
											<TextBlock Text="{loc:Loc Common.AppName}"
													   FontSize="14"
													   FontWeight="SemiBold"
													   Foreground="{StaticResource NeutralForegroundBrush}"
													   VerticalAlignment="Center"/>
											<TextBlock Text="{Binding Version}"
													   FontSize="12"
													   Foreground="{StaticResource NeutralForegroundSecondaryBrush}"
													   Margin="12 0 0 0"
													   VerticalAlignment="Center"/>
										</StackPanel>

										<!-- Window Controls -->
										<StackPanel Grid.Column="2"
													Orientation="Horizontal"
													VerticalAlignment="Top">
											<Button Style="{StaticResource FluentTitleBarButton}"
													Command="{Binding ReportBugCommand}"
													ToolTip="{loc:Loc MainWindow.ReportBug}">
												<TextBlock Text="&#xebe8;"
														   FontFamily="Segoe Fluent Icons"
														   FontSize="14"/>
											</Button>
											<Button Style="{StaticResource FluentTitleBarButton}"
													Click="HelpButton_Click"
													ToolTip="{loc:Loc MainWindow.Help}">
												<TextBlock Text="&#xE897;"
														   FontFamily="Segoe Fluent Icons"
														   FontSize="14"/>
											</Button>
											<Button Style="{StaticResource FluentTitleBarButton}"
													Click="MinButton_Click"
													ToolTip="{loc:Loc MainWindow.Minimize}">
												<TextBlock Text="&#xE921;"
														   FontFamily="Segoe Fluent Icons"
														   FontSize="14"/>
											</Button>
											<Button Style="{StaticResource FluentTitleBarButton}"
													Click="MaxButton_Click"
													ToolTip="{loc:Loc MainWindow.Maximize}">
												<TextBlock Text="&#xE922;"
														   FontFamily="Segoe Fluent Icons"
														   FontSize="14"/>
											</Button>
											<Button Style="{StaticResource FluentCloseButton}"
													Click="CloseButton_Click"
													ToolTip="{loc:Loc MainWindow.Close}">
												<TextBlock Text="&#xE8BB;"
														   FontFamily="Segoe Fluent Icons"
														   FontSize="14"/>
											</Button>
										</StackPanel>
									</Grid>
								</Border>

								<!-- Content Area -->
								<ContentPresenter Name="presenter"
												  Grid.Row="1"/>
								<cmp:ImagePreviewOverlay Grid.Row="1"/>
								<cmp:VersionCheckDetailOverlay Grid.Row="1"/>
								<cmp:ModConflictDetailOverlay Grid.Row="1"/>
								<cmp:MessageBox Grid.Row="1"/>
								<cmp:FirstRunTutorialOverlay Grid.Row="1"/>
							<cmp:ToastOverlay Grid.Row="1"/>

								<!-- 拖拽导入提示覆盖层：拖入压缩包时提示松开导入（模板内元素，code-behind 经 Template.FindName 访问） -->
								<Border x:Name="dropHintOverlay"
										Grid.RowSpan="2"
										Visibility="Collapsed"
										IsHitTestVisible="False"
										Background="#D9141414"
										BorderBrush="{StaticResource SystemAccentBrush}"
										BorderThickness="2"
										CornerRadius="12"
										Margin="32">
									<Grid>
										<!-- 有效拖拽：松开导入 -->
										<StackPanel x:Name="dropHintValidPanel"
													HorizontalAlignment="Center"
													VerticalAlignment="Center">
											<TextBlock Text="&#xE896;"
													   FontFamily="Segoe Fluent Icons"
													   FontSize="48"
													   Foreground="{StaticResource SystemAccentBrush}"
													   HorizontalAlignment="Center"/>
											<TextBlock Text="{loc:Loc MainWindow.DropImportHint}"
													   FontSize="18"
													   FontWeight="SemiBold"
													   Foreground="{StaticResource NeutralForegroundBrush}"
													   HorizontalAlignment="Center"
													   Margin="0 16 0 0"/>
										</StackPanel>
										<!-- 无效拖拽：不支持的格式 -->
										<StackPanel x:Name="dropHintInvalidPanel"
													HorizontalAlignment="Center"
													VerticalAlignment="Center"
													Visibility="Collapsed">
											<TextBlock Text="&#xE711;"
													   FontFamily="Segoe Fluent Icons"
													   FontSize="48"
													   Foreground="{StaticResource DangerBrush}"
													   HorizontalAlignment="Center"/>
											<TextBlock Text="{loc:Loc MainWindow.DropImportInvalid}"
													   FontSize="18"
													   FontWeight="SemiBold"
													   Foreground="{StaticResource NeutralForegroundBrush}"
													   HorizontalAlignment="Center"
													   Margin="0 16 0 0"/>
										</StackPanel>
									</Grid>
								</Border>
							</Grid>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="WindowState" Value="Maximized">
								<Setter TargetName="border" Property="Padding" Value="0"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Window.Style>

	<WindowChrome.WindowChrome>
		<WindowChrome CaptionHeight="48"
					  CornerRadius="0"
					  ResizeBorderThickness="6"/>
	</WindowChrome.WindowChrome>
</Window>
