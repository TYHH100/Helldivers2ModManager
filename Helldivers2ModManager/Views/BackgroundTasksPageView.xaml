<UserControl x:Class="Helldivers2ModManager.Views.BackgroundTasksPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:Helldivers2ModManager.Models"
             xmlns:loc="clr-namespace:Helldivers2ModManager.Extensions"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="BackgroundTaskTemplate" DataType="{x:Type models:BackgroundTaskItem}">
            <Border Background="#2a2a4a" CornerRadius="8" Padding="12" Margin="0 0 0 8"
                    Tag="{Binding DataContext, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}">
                <Border.ContextMenu>
                    <ContextMenu MinWidth="160">
                        <MenuItem Header="{loc:Loc BackgroundTasksPage.RemoveTask}"
                                  Command="{Binding PlacementTarget.Tag.RemoveTaskCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}}"
                                  CommandParameter="{Binding}">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                    </ContextMenu>
                </Border.ContextMenu>
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="&#xE9F5;" FontFamily="Segoe Fluent Icons" FontSize="22" Foreground="#6a9fd8" Margin="0 0 12 0" VerticalAlignment="Top"/>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Name}" FontSize="14" Foreground="White" Margin="0 0 0 4" />

                            <Border Background="#3a3a5a" CornerRadius="3" Height="6" Margin="0 0 0 4" ClipToBounds="True">
                                <Grid>
                                    <Rectangle Fill="#4CAF50" Height="6" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Rectangle.Style>
                                            <Style TargetType="Rectangle">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsIndeterminate}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Rectangle.Style>
                                        <Rectangle.Width>
                                            <MultiBinding Converter="{StaticResource ProgressWidthConverter}">
                                                <Binding Path="Progress" />
                                                <Binding RelativeSource="{RelativeSource AncestorType={x:Type Border}}" Path="ActualWidth" />
                                            </MultiBinding>
                                        </Rectangle.Width>
                                    </Rectangle>
                                </Grid>
                            </Border>

                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#888" />
                                <TextBlock Text="{Binding ProgressText}" FontSize="12" Foreground="#888" Margin="12 0 0 0" />
                            </StackPanel>

                            <TextBlock Text="{Binding ErrorMessage}" FontSize="12" Foreground="#f44336" Margin="0 4 0 0"
                                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding Status, Converter={StaticResource BackgroundTaskStatusToStringConverter}}" FontSize="12" Foreground="#888" HorizontalAlignment="Right" />
                            <TextBlock Text="{Binding StartedAt, StringFormat={}{0:HH:mm:ss}}" FontSize="11" Foreground="#666" HorizontalAlignment="Right" Margin="0 4 0 0" />
                        </StackPanel>
                    </Grid>

                    <!-- 步骤列表（与部署进度弹窗一致）：顶部最新、正在部署高亮，副标题显示复制进度 -->
                    <ScrollViewer MaxHeight="140"
                                  Margin="0 8 0 0"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  ScrollChanged="StepsScrollViewer_OnScrollChanged">
                        <ScrollViewer.Style>
                            <Style TargetType="ScrollViewer">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Steps.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ScrollViewer.Style>
                        <ItemsControl ItemsSource="{Binding Steps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:TaskStepItem}">
                                    <StackPanel Orientation="Horizontal" Margin="0 2">
                                        <TextBlock Text="{loc:Loc MessageBox.Deploying}"
                                                   Foreground="{StaticResource SystemAccentBrush}"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Top"
                                                   Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <TextBlock Text="✓ "
                                                   Foreground="#777777"
                                                   VerticalAlignment="Top"
                                                   Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <TextBlock Text="✗ "
                                                   Foreground="#f44336"
                                                   VerticalAlignment="Top"
                                                   FontWeight="SemiBold"
                                                   Visibility="{Binding IsFailed, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Text}"
                                                       Foreground="#CCCCCC"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Detail}"
                                                       FontSize="11"
                                                       Foreground="#888888"
                                                       TextTrimming="CharacterEllipsis"
                                                       Visibility="{Binding Detail, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="0 0 0 20" BorderThickness="0 0 0 1" BorderBrush="#333">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Command="{Binding GoBackCommand}"
                        Style="{StaticResource FluentIconButton}"
                        Margin="4,3,0,4" HorizontalAlignment="Left" Width="36">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72B;" FontFamily="Segoe Fluent Icons" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock ToolTip="{loc:Loc Common.Back}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0 6 0 6">
                    <TextBlock Text="{loc:Loc DashboardPage.BackgroundTasks}" FontSize="20" Foreground="White" />
                    <TextBlock Text="{loc:Loc BackgroundTasksPage.Subtitle}" FontSize="12" Foreground="#888" Margin="0 2 0 0" />
                </StackPanel>

                <Button Grid.Column="2"
                        Command="{Binding ClearCompletedCommand}"
                        IsEnabled="{Binding HasCompletedTasks}"
                        Style="{StaticResource FluentIconButton}"
                        Margin="0,3,4,4" HorizontalAlignment="Right"
                        ToolTip="{loc:Loc BackgroundTasksPage.ClearCompleted}">
                    <TextBlock Text="&#xE74D;" FontFamily="Segoe Fluent Icons" FontSize="14" VerticalAlignment="Center"/>
                </Button>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding VisibleTasks}" ItemTemplate="{StaticResource BackgroundTaskTemplate}">
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding VisibleTasks.Count}" Value="0">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 40 0 0">
                                                    <TextBlock Text="{loc:Loc BackgroundTasksPage.EmptyHint}" FontSize="14" Foreground="#666" />
                                                    <TextBlock Text="{loc:Loc BackgroundTasksPage.EmptyDesc}" FontSize="12" Foreground="#444" Margin="0 4 0 0" />
                                                </StackPanel>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
