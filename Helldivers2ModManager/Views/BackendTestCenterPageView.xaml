<Page x:Class="Helldivers2ModManager.Views.BackendTestCenterPageView"
      x:ClassModifier="internal"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:loc="clr-namespace:Helldivers2ModManager.Extensions"
      mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1180">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding GoBackCommand}" Style="{StaticResource FluentIconButton}" ToolTip="{loc:Loc Common.Back}" Margin="0 0 12 0">
                <TextBlock Text="&#xE72B;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
            </Button>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{loc:Loc BackendTestCenter.Title}" FontSize="22" FontWeight="SemiBold"/>
                <TextBlock Text="{loc:Loc BackendTestCenter.Description}" Margin="0 4 0 0" FontSize="12" Foreground="{StaticResource NeutralForegroundSecondaryBrush}" TextWrapping="Wrap"/>
            </StackPanel>
            <Button Grid.Column="2" Command="{Binding OpenLogsCommand}" Style="{StaticResource FluentSecondaryButton}" Padding="14 8" IsEnabled="{Binding IsReady}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE7B3;" FontFamily="Segoe Fluent Icons" Margin="0 0 7 0"/>
                    <TextBlock Text="{loc:Loc BackendTestCenter.OpenLogs}"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0 0 14 0">
                <StackPanel>
                    <Border Style="{StaticResource FluentSettingsCard}" Padding="14" Margin="0 0 0 12">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc BackendTestCenter.StatusTitle}" FontWeight="SemiBold" Margin="0 0 8 0"/>
                            <TextBlock Text="{Binding StorageStatus}" Margin="0 2 0 0" FontSize="12" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding GameStatus}" Margin="0 2 0 0" FontSize="12" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding ModCountStatus}" Margin="0 2 0 0" FontSize="12" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding DeploymentMode}" Margin="0 2 0 0" FontSize="12" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource FluentSettingsCard}" Padding="14" Margin="0 0 0 12">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc BackendTestCenter.ModActions}" FontWeight="SemiBold" Margin="0 0 10 0"/>
                            <Button Command="{Binding ImportArchiveCommand}" Style="{StaticResource FluentSecondaryButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0 0 0 8" IsEnabled="{Binding IsReady}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE8E5;" FontFamily="Segoe Fluent Icons" Margin="0 0 8 0"/>
                                    <TextBlock Text="{loc:Loc BackendTestCenter.ImportArchive}"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding RescanModsCommand}" Style="{StaticResource FluentSecondaryButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0 0 0 8" IsEnabled="{Binding IsReady}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons" Margin="0 0 8 0"/>
                                    <TextBlock Text="{loc:Loc BackendTestCenter.RescanMods}"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding CheckVersionsCommand}" Style="{StaticResource FluentSecondaryButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" IsEnabled="{Binding IsReady}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE9D9;" FontFamily="Segoe Fluent Icons" Margin="0 0 8 0"/>
                                    <TextBlock Text="{loc:Loc BackendTestCenter.CheckVersions}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource FluentSettingsCard}" Padding="14" Margin="0 0 0 12">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc BackendTestCenter.AnalysisActions}" FontWeight="SemiBold" Margin="0 0 10 0"/>
                            <Button Command="{Binding ScanConflictsCommand}" Style="{StaticResource FluentSecondaryButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0 0 0 8" IsEnabled="{Binding IsReady}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons" Margin="0 0 8 0"/>
                                    <TextBlock Text="{loc:Loc BackendTestCenter.ScanConflicts}"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding ScanArmorReuseCommand}" Style="{StaticResource FluentSecondaryButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" IsEnabled="{Binding IsReady}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE7BA;" FontFamily="Segoe Fluent Icons" Margin="0 0 8 0"/>
                                    <TextBlock Text="{loc:Loc BackendTestCenter.ScanArmorReuse}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource FluentSettingsCard}" Padding="14" Margin="0 0 0 12">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc BackendTestCenter.DeployActions}" FontWeight="SemiBold" Margin="0 0 10 0"/>
                            <Button Command="{Binding DeployEnabledModsCommand}" Style="{StaticResource FluentSecondaryButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" IsEnabled="{Binding CanDeploy}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE896;" FontFamily="Segoe Fluent Icons" Margin="0 0 8 0"/>
                                    <TextBlock Text="{loc:Loc BackendTestCenter.DeployEnabledMods}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource FluentSettingsCard}" Padding="14">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc BackendTestCenter.PageLinks}" FontWeight="SemiBold" Margin="0 0 10 0"/>
                            <UniformGrid Columns="2">
                                <Button Content="{loc:Loc DashboardPage.Title}" Command="{Binding OpenDashboardCommand}" Style="{StaticResource FluentSecondaryButton}" Margin="0 0 8 8"/>
                                <Button Content="{loc:Loc PatchResourceViewerPage.Title}" Command="{Binding OpenResourceViewerCommand}" Style="{StaticResource FluentSecondaryButton}" Margin="0 0 0 8"/>
                                <Button Content="{loc:Loc ModelPreviewPage.Title}" Command="{Binding OpenModelPreviewCommand}" Style="{StaticResource FluentSecondaryButton}" Margin="0 0 8 0"/>
                                <Button Content="{loc:Loc DashboardPage.Settings}" Command="{Binding OpenSettingsCommand}" Style="{StaticResource FluentSecondaryButton}"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Column="1" Style="{StaticResource FluentSettingsCard}" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0 0 0 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Loc BackendTestCenter.LogTitle}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding BusyText}" Foreground="{StaticResource SystemAccentBrush}" FontSize="12"/>
                    </Grid>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Logs}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.ScrollUnit="Pixel">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Message}" Margin="2 3 8 3" FontSize="12" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
