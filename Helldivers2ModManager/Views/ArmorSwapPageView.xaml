<Page x:Class="Helldivers2ModManager.Views.ArmorSwapPageView"
      x:ClassModifier="internal"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:loc="clr-namespace:Helldivers2ModManager.Extensions"
      mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding GoBackCommand}" Style="{StaticResource FluentIconButton}" ToolTip="{loc:Loc Common.Back}" Margin="0 0 12 0">
                <TextBlock Text="&#xE72B;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
            </Button>
            <StackPanel Grid.Column="1">
                <TextBlock Text="{loc:Loc ArmorSwapPage.Title}" FontSize="22" FontWeight="SemiBold"/>
                <TextBlock Text="{loc:Loc ArmorSwapPage.Description}" Margin="0 4 0 0" FontSize="12" Foreground="{StaticResource NeutralForegroundSecondaryBrush}" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Border Visibility="{Binding EmptyVisibility}" Padding="18" CornerRadius="6" BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1" Background="#15107C10" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="{loc:Loc ArmorSwapPage.NoSourceMods}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock Text="{loc:Loc ArmorSwapPage.EmptyDescription}" Margin="0 4 0 0" FontSize="12" TextWrapping="Wrap" Foreground="{StaticResource NeutralForegroundSecondaryBrush}"/>
                </StackPanel>
            </Border>

            <ScrollViewer Visibility="{Binding ContentVisibility}" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource FluentSettingsCard}" Margin="0 0 0 12">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc ArmorSwapPage.SourceSection}" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="{loc:Loc ArmorSwapPage.SourceModLabel}" Margin="0 10 0 4" FontSize="12" Foreground="{StaticResource NeutralForegroundSecondaryBrush}"/>
                            <ComboBox ItemsSource="{Binding SourceMods}" SelectedItem="{Binding SelectedSourceMod}" DisplayMemberPath="Manifest.Name" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
                            <TextBlock Text="{loc:Loc ArmorSwapPage.SourceGroupLabel}" Margin="0 10 0 4" FontSize="12" Foreground="{StaticResource NeutralForegroundSecondaryBrush}"/>
                            <ComboBox ItemsSource="{Binding SourceGroups}" SelectedItem="{Binding SelectedSourceGroup}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayText}" TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <TextBlock Text="{loc:Loc ArmorSwapPage.TargetArmorLabel}" Margin="0 10 0 4" FontSize="12" Foreground="{StaticResource NeutralForegroundSecondaryBrush}"/>
                            <ComboBox ItemsSource="{Binding TargetArmors}" SelectedItem="{Binding SelectedTargetArmor}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding DisplayName}"/>
                                            <TextBlock Text="{Binding PollutionText}" FontSize="11" Foreground="{StaticResource SystemAccentBrush}" Visibility="{Binding PollutionText, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <TextBlock Text="{Binding SummaryText}" Margin="0 10 0 0" FontSize="12" TextWrapping="Wrap" Foreground="{StaticResource NeutralForegroundSecondaryBrush}"/>
                            <TextBlock Text="{Binding WarningsText}" Margin="0 6 0 0" FontSize="12" TextWrapping="Wrap" Foreground="{StaticResource WarningBrush}" Visibility="{Binding WarningsText, Converter={StaticResource StringToVisibilityConverter}}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource FluentSettingsCard}" Margin="0 0 0 12" Visibility="{Binding IssuesVisibility}">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc ArmorSwapPage.IssuesSection}" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="{loc:Loc ArmorSwapPage.NoErrorsNote}" Margin="0 8 0 0" FontSize="12" TextWrapping="Wrap" Foreground="{StaticResource SuccessBrush}" Visibility="{Binding NoErrorsVisibility}"/>
                            <ItemsControl ItemsSource="{Binding Issues}" Margin="0 8 0 0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0 3 0 0">
                                            <TextBlock Text="&#xE711;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="#DC5037" VerticalAlignment="Center" Visibility="{Binding IsError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <TextBlock Text="&#xE7BA;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{StaticResource WarningBrush}" VerticalAlignment="Center" Visibility="{Binding IsError, Converter={StaticResource InverseBoolConverter}}"/>
                                            <TextBlock Text="{Binding Text}" Margin="8 0 0 0" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Button Command="{Binding GenerateCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                            Style="{StaticResource FluentPrimaryButton}" HorizontalAlignment="Right" Padding="18 9">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8AB;" FontFamily="Segoe Fluent Icons" Margin="0 0 8 0"/>
                            <TextBlock Text="{loc:Loc ArmorSwapPage.Generate}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
