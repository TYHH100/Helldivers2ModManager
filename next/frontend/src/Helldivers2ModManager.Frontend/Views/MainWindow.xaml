<Window x:Class="Helldivers2ModManager.Frontend.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:vms="clr-namespace:Helldivers2ModManager.Frontend.ViewModels"
        xmlns:pages="clr-namespace:Helldivers2ModManager.Frontend.ViewModels.Pages"
        xmlns:views="clr-namespace:Helldivers2ModManager.Frontend.Views"
        Title="{Binding AppTitle}"
        Width="1280" Height="800"
        MinWidth="1080" MinHeight="680"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource BaseBrush}"
        AllowDrop="True"
        PreviewDragOver="OnPreviewDragOver"
        PreviewDrop="OnPreviewDrop"
        DragLeave="OnDragLeave">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/FrontendTokens.xaml"/>
                <ResourceDictionary Source="../Themes/FrontendControls.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type pages:LibraryPageViewModel}"><views:LibraryPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:DeploymentOrderPageViewModel}"><views:DeploymentOrderPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:BackgroundTasksPageViewModel}"><views:BackgroundTasksPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:CreatePageViewModel}"><views:CreatePageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:EditPageViewModel}"><views:EditPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:ManifestEditPageViewModel}"><views:ManifestEditPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:TagManagementPageViewModel}"><views:TagManagementPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:AutoTagPairingPageViewModel}"><views:AutoTagPairingPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:NexusDownloadPageViewModel}"><views:NexusDownloadPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:PatchResourceViewerPageViewModel}"><views:PatchResourceViewerPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:VersionCheckPageViewModel}"><views:VersionCheckPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:ConflictScanPageViewModel}"><views:ConflictScanPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:ArmorReusePageViewModel}"><views:ArmorReusePageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:ModelPreviewPageViewModel}"><views:ModelPreviewPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:BisectPageViewModel}"><views:BisectPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:DiagnosticsPageViewModel}"><views:DiagnosticsPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:SettingsPageViewModel}"><views:SettingsPageView/></DataTemplate>
            <DataTemplate DataType="{x:Type pages:HelpPageViewModel}"><views:HelpPageView/></DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Style>
        <Style TargetType="{x:Type Window}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Window}">
                        <Border BorderBrush="{DynamicResource StrokeBrush}" BorderThickness="1" Background="{DynamicResource BaseBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="44"/>
                                    <RowDefinition Height="52"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="#22FFFFFF" BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding AppTitle}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,6,8,0" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                            <Button Style="{StaticResource TitleBarButton}" Click="OnShowHelp">
                                                <TextBlock Text="?" FontFamily="Segoe UI" FontSize="14" FontWeight="Bold"/>
                                            </Button>
                                            <Button Style="{StaticResource TitleBarButton}" Click="OnMinimize">
                                                <Path Data="M 0,0 H10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1"/>
                                            </Button>
                                            <Button Style="{StaticResource TitleBarButton}" Click="OnMaximize">
                                                <Rectangle Width="10" Height="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1"/>
                                            </Button>
                                            <Button Style="{StaticResource CloseButton}" Click="OnClose">
                                                <Path Data="M 0,0 L10,10 M 10,0 L0,10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="#18FFFFFF" BorderThickness="0,0,0,1">
                                    <Grid Margin="16,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <ItemsControl ItemsSource="{Binding Modules}" VerticalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Style="{StaticResource ModuleTab}" Content="{Binding Title}" Tag="{Binding IsCurrent}"
                                                            Command="{Binding DataContext.SelectModuleCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding Key}"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource ModuleTab}" Command="{Binding OpenImportCommand}" ToolTip="{Binding ImportLabel}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE896;" FontFamily="Segoe Fluent Icons" FontSize="13" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding ImportLabel}" Margin="7,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                            <Button Style="{StaticResource ModuleTab}" Command="{Binding OpenDeploymentCommand}" ToolTip="{Binding DeployLabel}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE895;" FontFamily="Segoe Fluent Icons" FontSize="13" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding DeployLabel}" Margin="7,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                            <Button Style="{StaticResource ModuleTab}" Command="{Binding OpenTasksCommand}" ToolTip="{Binding TasksLabel}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE9D5;" FontFamily="Segoe Fluent Icons" FontSize="13" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding TasksLabel}" Margin="7,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                            <TextBox Style="{StaticResource SearchBox}" Width="230" Margin="10,0,0,0" Tag="{Binding SearchWatermark}"
                                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="224"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="#F2161C24" BorderBrush="#12FFFFFF" BorderThickness="0,0,1,0">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,14,0,14">
                                            <ItemsControl ItemsSource="{Binding CurrentSubPages}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Style="{StaticResource NavButton}" Content="{Binding Title}" Tag="{Binding IsCurrent}"
                                                                Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                CommandParameter="{Binding RouteKey}"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>

                                    <Grid Grid.Column="1" Margin="28,24,28,28">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Margin="4,2,4,20">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding CurrentModuleTitle}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                <TextBlock Text="/" Margin="8,0" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                <TextBlock Text="{Binding CurrentTitle}" FontSize="12" Foreground="{DynamicResource AccentBrush}"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding CurrentTitle}" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding CurrentDescription}" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>

                                        <ContentPresenter x:Name="PageHost" Grid.Row="1"
                                                          Content="{Binding CurrentPage, NotifyOnTargetUpdated=True}"
                                                          TargetUpdated="OnPageTargetUpdated"/>
                                    </Grid>
                                </Grid>

                                <Border x:Name="DropOverlay" Grid.RowSpan="3" Background="#D90E1116" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="2"
                                        CornerRadius="10" Margin="24" Visibility="Collapsed" IsHitTestVisible="False">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="&#xE896;" FontFamily="Segoe Fluent Icons" FontSize="44" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="DropOverlayText" Margin="0,14,0,0" FontSize="17" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>
</Window>
